# architecture mgr
#
# each arch plugin registers itself with arch mgr. each arch plugin defines
# one or more ArchInfo objects to handle arch- and os-specific settings.
#
# the arch mgr finds the correct archinfo object based on a string that
# names the arch/OS (e.g. from ident)
raise "#{__FILE__} : NOT IMPLEMENTED"
